{"default":{"identifier":"default","description":"Default capabilities for the Meridian main window plus the popped-out AI debug window. Both windows need the same core/opener/dialog grants; the main window additionally needs `core:webview:allow-create-webview-window` so it can spawn the debug window via `new WebviewWindow(...)`.","local":true,"windows":["main","ai-debug"],"permissions":["core:default","core:webview:allow-create-webview-window","opener:default","dialog:default"]}}